import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
	container: {
		flex: 1,
		marginBottom: 10,
		marginTop: 10,
	},
	image: {
		width: 120,
		height: 180,
		resizeMode: "cover",
		borderRadius: 5,
		margin: 5
	},
	title: {
		fontSize: 20,
		fontWeight: "bold",
		color: "white"
	}
})
export default styles;